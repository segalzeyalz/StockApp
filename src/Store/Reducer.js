import * as actionTypes from './Actions';

const initialState = {
    sharesName: ["S&P 500", "Dow 30", "Nasdaq", "Amazon.com", "Alphabet Inc"]
};

const Reducer = (state = initialState, action) => {
    switch (action.type) {

    }
    return state;
};

export default Reducer;